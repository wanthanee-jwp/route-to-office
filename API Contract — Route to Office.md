# API Contract — Route to Office

2026-09-18 · @Back-jahjha

## 1. เอกสารนี้คืออะไร

สัญญาระหว่าง backend กับ frontend ของระบบแสดงเส้นทางมายังบริษัท ทีม frontend ใช้เอกสารนี้เขียน mock และพัฒนาได้เลยโดยไม่ต้องรอ backend เสร็จ

สิ่งที่อยู่ในนี้คือหน้าตาของ request และ response เท่านั้น เหตุผลเบื้องหลังการออกแบบอยู่ในเอกสาร Requirement ซึ่งทีม frontend ไม่จำเป็นต้องอ่าน

**สถานะ**

| หัวข้อ | สถานะ |
| --- | --- |
| Endpoint และ schema | นิ่งแล้ว ใช้เขียน mock ได้ |
| ข้อความ error ภาษาไทย | ถ้อยคำอาจปรับได้ แต่ `code` จะไม่เปลี่ยน |
| Base URL | ยังไม่สรุป ขึ้นกับที่ deploy |

**สิ่งที่ frontend ไม่ต้องทำ**

สามอย่างนี้ backend จัดการให้หมดแล้ว ถ้าพบว่าต้องทำเองแปลว่ามีอะไรผิด ให้แจ้งกลับมา

- ไม่ต้องแปลงหน่วยหรือจัดรูปข้อความ ทุกค่ามี `text` ที่พร้อมแสดงมาให้
- ไม่ต้องรู้พิกัดบริษัท ดึงจาก `/api/company`
- ไม่ต้องตีความ HTTP status เพื่อสร้างข้อความเอง ใช้ `message` ที่ได้มาตรง ๆ

## 2. ข้อตกลงร่วม

**Base URL**

| Environment | Base URL |
| --- | --- |
| Local | `http://localhost:3000` |
| Staging | ยังไม่สรุป |
| Production | ยังไม่สรุป |

ทุก endpoint อยู่ใต้ `/api` ยกเว้น `/healthz`

**Header**

| Header | ค่า | ใช้เมื่อ |
| --- | --- | --- |
| `Content-Type` | `application/json` | ทุก request ที่มี body |
| `Accept` | `application/json` | ทุก request |

ไม่ต้องส่ง token หรือ API key ใด ๆ API นี้เปิดสาธารณะ ควบคุมการเข้าถึงด้วย CORS และ rate limit แทน

**ข้อตกลงเรื่องรูปแบบข้อมูล**

| เรื่อง | ข้อตกลง |
| --- | --- |
| ชื่อ field | camelCase ทั้งหมด ไม่มี snake\_case ที่ใดเลย |
| พิกัด | ทศนิยม องศา `lat` และ `lng` เป็น number ไม่ใช่ string |
| ระยะทาง | หน่วยเมตร เป็น integer |
| ระยะเวลา | หน่วยวินาที เป็น integer |
| วันเวลา | RFC 3339 พร้อม offset `+07:00` เสมอ |
| Field ที่ไม่มีค่า | ไม่ส่ง key นั้นมาเลย ไม่ส่ง `null` |

ข้อสุดท้ายสำคัญกับการเขียน TypeScript type ให้ประกาศ field ที่อาจไม่มีเป็น optional ไม่ใช่ nullable

**Timezone**

เวลาทุกค่าที่ส่งมาเป็นเวลาไทยพร้อม offset `+07:00` ติดมาด้วยเสมอ frontend แปลงเป็น `Date` ได้ตรง ๆ โดยไม่ต้องบวกลบเอง

## 3. GET /api/config

คืนค่าที่ frontend ต้องใช้ตอนเริ่มต้น เรียกครั้งเดียวตอนโหลดหน้า

**Request** — ไม่มีพารามิเตอร์

**Response 200**

```json
{
  "googleMapsBrowserKey": "AIzaSy...",
  "defaultZoom": 14
}
```

| Field | ชนิด | คำอธิบาย |
| --- | --- | --- |
| `googleMapsBrowserKey` | string | key สำหรับโหลด Maps JavaScript API |
| `defaultZoom` | number | ระดับ zoom เริ่มต้นตอนแสดงหมุดบริษัทก่อนมีเส้นทาง |

key ตัวนี้จำกัดด้วย HTTP referrer อยู่แล้ว ใช้ได้เฉพาะจากโดเมนที่อนุญาต จึงส่งมาให้ browser ได้โดยไม่ต้องกังวล แต่อย่านำไปใช้ที่อื่นนอกแอปนี้

เรียก endpoint นี้ก่อนโหลด Maps JS API เสมอ อย่า hardcode key ลงในโค้ด frontend เพราะ key ของแต่ละ environment ไม่เหมือนกัน

## 4. GET /api/company

คืนข้อมูลบริษัทสำหรับปักหมุดปลายทางและแสดงที่อยู่ เรียกครั้งเดียวตอนโหลดหน้า

**Request** — ไม่มีพารามิเตอร์

**Response 200**

```json
{
  "name": "บริษัท อินโนเวท เอไอ จำกัด",
  "address": "เลขที่ ... แขวง ... เขต ... กรุงเทพฯ 10xxx",
  "location": {
    "lat": 13.7563,
    "lng": 100.5018
  },
  "placeId": "ChIJxxxxxxxxxxxx"
}
```

| Field | ชนิด | คำอธิบาย |
| --- | --- | --- |
| `name` | string | ชื่อบริษัทสำหรับแสดงบนหมุดและการ์ด |
| `address` | string | ที่อยู่เต็มสำหรับแสดงใต้ชื่อ |
| `location.lat` | number | ละติจูดสำหรับปักหมุด |
| `location.lng` | number | ลองจิจูดสำหรับปักหมุด |
| `placeId` | string, optional | Google Place ID ใช้ทำปุ่มเปิดใน Google Maps ถ้าไม่มี key นี้ให้ใช้พิกัดแทน |

**ปุ่มเปิดใน Google Maps**

ถ้ามี `placeId` ให้ประกอบ URL แบบนี้ จะได้หน้าที่ตรงกับสถานที่จริง ไม่ใช่แค่จุดบนแผนที่

```
https://www.google.com/maps/search/?api=1&query=<lat>,<lng>&query_place_id=<placeId>
```

ถ้าไม่มี `placeId` ตัด `query_place_id` ออก

เรียก endpoint นี้ก่อนแสดงแผนที่ครั้งแรก อย่า hardcode พิกัดบริษัทไว้ใน frontend เพราะวันที่บริษัทย้ายออฟฟิศจะได้ไม่ต้อง build ใหม่

## 5. POST /api/route

endpoint หลัก รับพิกัดผู้ใช้แล้วคืนเส้นทางมายังบริษัท เรียกเมื่อผู้ใช้กดปุ่มค้นหาเส้นทางและได้พิกัดจาก Geolocation แล้ว

**Request**

```json
{
  "lat": 13.7465,
  "lng": 100.5350
}
```

| Field | ชนิด | บังคับ | เงื่อนไข |
| --- | --- | --- | --- |
| `lat` | number | ใช่ | -90 ถึง 90 |
| `lng` | number | ใช่ | -180 ถึง 180 |

ส่งได้เฉพาะสอง field นี้ field อื่นจะถูกปฏิเสธด้วย 422 รวมถึง `accuracy` ที่เคยมีในร่างก่อนหน้า ถ้าอยากใช้ `accuracy` เตือนผู้ใช้เรื่องความแม่นยำ ให้เก็บไว้ใน frontend ไม่ต้องส่งมา

ปลายทางไม่ต้องส่ง backend อ่านจาก config เอง

**Response 200**

```json
{
  "distance": { "meters": 12400, "text": "12.4 กม." },
  "duration": { "seconds": 1680, "text": "28 นาที" },
  "staticDuration": { "seconds": 1200, "text": "20 นาที" },
  "trafficDelaySeconds": 480,
  "polyline": "ktp~AqvyhVrAaB...",
  "origin": { "lat": 13.7465, "lng": 100.5350 },
  "destination": { "lat": 13.7563, "lng": 100.5018 },
  "computedAt": "2026-09-18T14:32:10+07:00",
  "cached": false
}
```

| Field | ชนิด | คำอธิบาย |
| --- | --- | --- |
| `distance.meters` | number | ระยะทางตามถนน หน่วยเมตร |
| `distance.text` | string | ข้อความพร้อมแสดง เช่น `12.4 กม.` หรือ `850 ม.` |
| `duration.seconds` | number | เวลาที่ใช้จริง คิดสภาพจราจรแล้ว |
| `duration.text` | string | ข้อความพร้อมแสดง เช่น `28 นาที` หรือ `1 ชม. 15 นาที` |
| `staticDuration.seconds` | number | เวลาถ้าไม่มีรถติด ใช้เทียบให้เห็นว่าตอนนี้แย่กว่าปกติแค่ไหน |
| `staticDuration.text` | string | ข้อความพร้อมแสดง |
| `trafficDelaySeconds` | number | ส่วนต่างของสองค่าข้างบน เป็น 0 เมื่อไม่ช้ากว่าปกติ |
| `polyline` | string | encoded polyline ของเส้นทาง ดูวิธีใช้ในหัวข้อ 9 |
| `origin` | object | พิกัดที่ส่งมา สะท้อนกลับเพื่อให้ปักหมุดต้นทางได้ |
| `destination` | object | พิกัดบริษัท ตรงกับที่ได้จาก `/api/company` |
| `computedAt` | string | เวลาที่คำนวณ ใช้แสดงว่าข้อมูลสดแค่ไหน |
| `cached` | boolean | `true` เมื่อผลมาจาก cache ภายใน 5 นาที |

**การแสดงสถานะจราจร**

ใช้ `trafficDelaySeconds` ตัดสินว่าจะแสดงคำเตือนหรือไม่ เกณฑ์ที่แนะนำคือเกิน 300 วินาที จึงแสดงว่ารถติดกว่าปกติกี่นาที ต่ำกว่านั้นไม่ต้องแสดงอะไร เพราะความคลาดเคลื่อนระดับไม่กี่นาทีเป็นเรื่องปกติ

**เวลาที่คาดว่าจะถึง**

คำนวณจาก `computedAt` บวก `duration.seconds` ไม่ใช่จากเวลาปัจจุบันของเครื่องผู้ใช้ เพราะถ้าผลมาจาก cache เวลาปัจจุบันอาจห่างจากตอนคำนวณไปหลายนาที

**Status ที่เป็นไปได้**

| Status | ความหมาย |
| --- | --- |
| 200 | สำเร็จ |
| 422 | พิกัดไม่ถูกต้อง หรืออยู่ไกลเกินไป |
| 404 | ไม่พบเส้นทาง |
| 429 | เรียกถี่เกินไป |
| 502 | ระบบเส้นทางขัดข้อง |
| 504 | ใช้เวลานานเกินไป |

## 6. GET /healthz

สำหรับระบบ deploy ตรวจว่า service พร้อมทำงาน frontend ไม่ต้องเรียก

**Response 200**

```json
{ "status": "ok" }
```

ไม่มี rate limit และไม่นับรวมในโควต้าใด ๆ

## 7. รูปแบบ Error

ทุก error ใช้โครงเดียวกันหมด ไม่ว่าจะ status ใด

```json
{
  "statusCode": 422,
  "code": "LOCATION_TOO_FAR",
  "message": "ตำแหน่งของคุณอยู่ไกลเกินกว่าจะคำนวณเส้นทางมายังบริษัทได้",
  "requestId": "7f3a1c9e"
}
```

| Field | ใช้ทำอะไร |
| --- | --- |
| `statusCode` | ตรงกับ HTTP status ใช้ตัดสินใจเชิงโปรแกรม |
| `code` | รหัสคงที่ ใช้เขียนเงื่อนไขใน frontend ไม่เปลี่ยนแม้ข้อความจะปรับ |
| `message` | ข้อความภาษาไทยพร้อมแสดงต่อผู้ใช้ทันที |
| `requestId` | ใช้แจ้ง backend เวลามีปัญหา ควรแสดงตัวเล็ก ๆ ในหน้า error |

**เขียนเงื่อนไขด้วย `code` ไม่ใช่ `message`** เพราะข้อความอาจถูกปรับถ้อยคำเมื่อไรก็ได้ แต่ `code` เป็นส่วนหนึ่งของสัญญา

**รหัสทั้งหมด**

| `code` | Status | ความหมาย | frontend ควรทำ |
| --- | --- | --- | --- |
| `INVALID_COORDINATES` | 422 | พิกัดผิดรูปหรือนอกช่วง | แสดง message พร้อมปุ่มลองใหม่ ตรวจว่าส่งค่าถูกชนิด |
| `LOCATION_TOO_FAR` | 422 | อยู่ห่างบริษัทเกิน 500 กม. | แสดง message ไม่ต้องมีปุ่มลองใหม่ เพราะลองกี่ครั้งก็เหมือนเดิม |
| `ROUTE_NOT_FOUND` | 404 | ไม่มีเส้นทางถนนไปถึง | แสดง message พร้อมปุ่มเปิดใน Google Maps |
| `RATE_LIMITED` | 429 | เรียกถี่เกินไป | แสดง message ปิดปุ่มตามค่าใน header `Retry-After` |
| `UPSTREAM_ERROR` | 502 | ระบบเส้นทางขัดข้อง | แสดง message พร้อมปุ่มลองใหม่ |
| `UPSTREAM_TIMEOUT` | 504 | ใช้เวลานานเกินไป | แสดง message พร้อมปุ่มลองใหม่ |

**ตัวอย่างข้อความจริง**

| `code` | `message` |
| --- | --- |
| `INVALID_COORDINATES` | ไม่สามารถอ่านตำแหน่งของคุณได้ กรุณาลองใหม่อีกครั้ง |
| `LOCATION_TOO_FAR` | ตำแหน่งของคุณอยู่ไกลเกินกว่าจะคำนวณเส้นทางมายังบริษัทได้ |
| `ROUTE_NOT_FOUND` | ไม่พบเส้นทางรถยนต์จากตำแหน่งของคุณมายังบริษัท |
| `RATE_LIMITED` | มีการเรียกใช้งานถี่เกินไป กรุณารอสักครู่แล้วลองใหม่ |
| `UPSTREAM_ERROR` | ระบบค้นหาเส้นทางขัดข้องชั่วคราว กรุณาลองใหม่อีกครั้ง |
| `UPSTREAM_TIMEOUT` | การค้นหาเส้นทางใช้เวลานานเกินไป กรุณาลองใหม่อีกครั้ง |

**รหัสที่ไม่มีในตาราง**

ถ้าเจอ `code` ที่ไม่รู้จัก ให้แสดง `message` ที่ได้มาตามปกติ อย่า crash และอย่าแสดงข้อความ fallback ของตัวเอง เพราะ `message` ที่ส่งมาพร้อมแสดงได้เสมอ

## 8. Rate limit, CORS และ Timeout

**Rate limit**

| ขอบเขต | ลิมิต |
| --- | --- |
| ต่อ IP | 20 request ต่อนาที |
| ทั้งระบบ | 500 request ต่อนาที |

เมื่อโดน 429 จะมี header `Retry-After` เป็นจำนวนวินาทีที่ต้องรอ ให้ปิดปุ่มตามเวลานั้นแทนที่จะให้ผู้ใช้กดรัวแล้วโดนซ้ำ

การใช้งานปกติไม่มีทางชน เพราะผู้ใช้หนึ่งคนกดไม่กี่ครั้ง แต่ระวังอย่าเขียน retry อัตโนมัติแบบวนซ้ำเร็ว ๆ เพราะจะชนลิมิตเอง ถ้าจะ retry ให้เว้นระยะเพิ่มขึ้นเรื่อย ๆ และไม่เกิน 3 ครั้ง

**CORS**

Backend อนุญาตเฉพาะโดเมนของ frontend ที่ลงทะเบียนไว้ ไม่ได้เปิดกว้าง ถ้าเพิ่มโดเมนใหม่หรือเปลี่ยน subdomain ต้องแจ้ง backend ล่วงหน้า ไม่งั้น request จะถูกปฏิเสธที่ชั้น browser ก่อนถึง server ด้วยซ้ำ

ระหว่างพัฒนา `http://localhost:3000` และ `http://localhost:5173` อยู่ในรายการอนุญาตแล้ว ถ้าใช้ port อื่นบอกมาได้

**Timeout**

| จุด | ค่า |
| --- | --- |
| Backend รอ Google | 8 วินาที |
| Client ควรตั้ง | 15 วินาที |

ตั้ง client timeout ที่ 15 วินาทีเพื่อให้ยาวกว่าฝั่ง server ถ้าตั้งสั้นกว่า จะเห็นเป็น timeout ฝั่ง client ทั้งที่ server กำลังจะตอบ 504 พร้อมข้อความที่ใช้ได้อยู่แล้ว

**Cache**

ผลลัพธ์ถูก cache 5 นาทีตามพื้นที่ ถ้าผู้ใช้กดซ้ำจากที่เดิมจะได้ `cached: true` และตอบเร็วมาก ไม่ต้องทำ cache ซ้อนอีกชั้นใน frontend

## 9. การใช้ polyline

`polyline` ที่ส่งมาเป็น encoded polyline ของ Google ไม่ใช่ array ของพิกัด ต้อง decode ก่อนวาด

**วิธี decode**

ถ้าโหลด Maps JavaScript API พร้อม library `geometry` แล้ว ใช้ได้เลย

```js
const path = google.maps.geometry.encoding.decodePath(response.polyline);

new google.maps.Polyline({
  path,
  map,
  strokeWeight: 5,
});
```

ต้องใส่ `libraries=geometry` ตอนโหลด Maps JS API ไม่งั้น `decodePath` จะไม่มีให้ใช้ ข้อนี้พลาดกันบ่อยเพราะ error ที่ได้ไม่ได้บอกตรง ๆ

ถ้าไม่ได้ใช้ Google Maps มีไลบรารีอย่าง `@mapbox/polyline` ที่ decode รูปแบบเดียวกันได้

**การจัดกรอบแผนที่**

หลังวาดเส้นแล้วให้ขยับกรอบให้เห็นทั้งเส้น พร้อม padding กันหมุดติดขอบ

```js
const bounds = new google.maps.LatLngBounds();
path.forEach(p => bounds.extend(p));
map.fitBounds(bounds, 50);
```

**ข้อควรระวัง**

อย่าลากเส้นตรงระหว่าง `origin` กับ `destination` เอง เพราะเส้นทางจริงอ้อมไปตามถนน ถ้าลากตรงผู้ใช้จะเห็นเส้นที่ขัดกับตัวเลขระยะทางที่แสดงอยู่ข้าง ๆ

## 10. การเปลี่ยนสัญญา

**สิ่งที่เปลี่ยนได้โดยไม่ต้องแจ้ง**

- ถ้อยคำใน `message`
- ค่าของ `defaultZoom`
- เพิ่ม field ใหม่เข้ามาใน response

เขียน frontend ให้ทนต่อ field ที่ไม่รู้จัก อย่าใช้ validation แบบเข้มที่ reject ทั้ง response เมื่อเจอ key เกิน

**สิ่งที่ต้องแจ้งและตกลงร่วมกันก่อน**

- เพิ่ม ลบ หรือเปลี่ยนชื่อ field ที่มีอยู่
- เปลี่ยนชนิดข้อมูลของ field
- เพิ่มหรือเปลี่ยน `code` ของ error
- เปลี่ยน HTTP status ที่คู่กับ `code` เดิม
- เปลี่ยน path ของ endpoint

**ช่องทาง**

คนแก้สัญญาต้องอัปเดตเอกสารนี้แล้วแจ้งอีกทีมทันที ไม่ใช่แก้โค้ดก่อนแล้วค่อยตามมาแก้เอกสาร

เมื่อ backend ขึ้น staging แล้ว จะมี Swagger UI ให้ลองยิงจริงได้ ซึ่งสร้างจากโค้ดโดยตรง ถ้าเอกสารนี้กับ Swagger ไม่ตรงกัน ให้ยึด Swagger แล้วแจ้งกลับมาให้แก้เอกสาร

**สิ่งที่ยังไม่สรุป**

| เรื่อง | สถานะ |
| --- | --- |
| Base URL ของ staging และ production | รอสรุปที่ deploy |
| โดเมนของ frontend ที่ต้องใส่ใน CORS | รอทีม frontend แจ้ง |
| รองรับภาษาอังกฤษหรือไม่ | ถ้าต้องรองรับ จะเพิ่มการรับ locale แล้วส่ง `message` ตามภาษาที่ขอ |

สองข้อแรกไม่บล็อกการเริ่มพัฒนา ใช้ mock ตามสัญญานี้ไปก่อนได้เลย
